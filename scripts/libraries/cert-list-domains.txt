# scripts/libraries/cert-list-domains.txt
# (Đổi tên từ cert-domains.txt)
#
# Danh sách domain cần inject cert vào apisix-{DC_PROFILE}.yaml
# Dùng bởi: scripts/runtime/inject-certs.sh (sh + GNU sed, gitsync container)
#
# Convention output trong ./certs/ (sau 2-decrypt-certs.sh):
#   <domain>.cert   (plaintext PEM public)
#   <domain>.key    (plaintext PEM private)
#
# Sync với: scripts/libraries/decrypt-cert-helper.sh (CERT_DOMAINS array)
# Khi thêm domain mới → sửa CẢ 2 file này
# =========================================================================


# ── VAULT INTEGRATION (uncomment khi có thông tin Vault) ─────────────────
# Khi chuyển sang Vault, file này sẽ không còn cần thiết.
# inject-certs.sh sẽ được thay bằng APISIX Vault secret provider.
# Xem hướng dẫn trong scripts/libraries/decrypt-cert-helper.sh
# ─────────────────────────────────────────────────────────────────────────

infiniband.vn
sds.infiniband.vn
s3-hcm.sds.infiniband.vn
s3-hni.sds.infiniband.vn